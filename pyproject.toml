[project]
name = "oscarator-com"
version = "0.1.0"
description = "Share your Oscar predictions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0",
]

[dependency-groups]
dev = [
    "djade>=1.7.0",
    "ruff>=0.14.9",
]
